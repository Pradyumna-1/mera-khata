<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Note-Taking</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10 text-white">
        <form action="/create" method="post">
          <input
            type="text"
            name="title"
            placeholder="Enter your task Name"
            class="bg-zinc-700 w-full block outline-none rounded-md px-5 py-2 text-white"
          />
          <textarea
            name="details"
            class="block w-full rounded-md outline-none mt-2 px-5 py-2 resize-none bg-zinc-700 text-white text-lg"
            placeholder="Write the task details here..."
          ></textarea>
          <input
            class="mt-2 bg-blue-700 px-2 py-2 rounded-md"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="task flex gap-3 flex-wrap p-10">
        <% if (files.length > 0) { %> <% files.forEach(function(val) { %>
        <div class="task w-75 px-3 py-4 rounded-md bg-zinc-700 overflow-hidden">
          <h1 class="text-white text-3xl tracking-tighter break-words">
            <%= val %>
          </h1>
        <div class="flex w-full justify-between item-center mt-3">
          <a class="text-blue-500 inline-block" href="/files/<%=val  %>">Read more</a>
          <a class="text-zinc-300" href="/edit/<%= val %>">edit</a>
        </div>
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600">No tasks yet.</h3>
        <% } %>
      </div>
    </div>
  </body>
</html>
